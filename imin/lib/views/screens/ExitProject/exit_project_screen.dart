import 'package:flutter/material.dart';

class ExitProjectScreen extends StatelessWidget {
  const ExitProjectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'ExitProjectScreen',
      style: TextStyle(
        color: Colors.black,
        fontSize: 36,
      ),
    );
  }
}
